fun main() {
    var neverNull: String = "this var cannot be null"
    var canBeNull = "this var can be null"
    var nullable: String? = "this one can also be null"
    println(neverNull)
    println(canBeNull)
    println(nullable)
}